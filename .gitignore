node_modules/
dist/
*.local
.DS_Store

# media-src/ and refs/ ARE committed. The client's originals are
# irreplaceable and must not live only on one machine, and committing them
# keeps `npm run prepare-media` reproducible from a fresh clone.
